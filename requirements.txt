# ============================================================
# Omiaちゃん Blog — Python 依赖清单
# ============================================================
# 安装方式: pip install -r requirements.txt
# 最低 Python 版本: 3.9+

# --- Flask Web 框架 ---
Flask==3.1.0
flask-cors==5.0.1

# --- 认证令牌签名（Flask 内置依赖，显式锁定版本） ---
itsdangerous==2.2.0

# --- RinUI — PySide6/QML Fluent Design UI 库 ---
RinUI>=0.4.0

# --- PySide6 — Qt for Python（RinUI 运行时依赖） ---
PySide6==6.8.3
PySide6_Essentials==6.8.3
PySide6_Addons==6.8.3

# --- WSGI 生产服务器（纯 Python，Windows 兼容） ---
waitress==3.0.2

# --- 开发工具 ---
python-dotenv==1.1.0
